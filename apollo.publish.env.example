# Copy to `apollo.publish.env` (gitignored) and fill values.
# cp apollo.publish.env.example apollo.publish.env
#
# APOLLO_KEY: Graph API key from Apollo Studio (Users & API Keys → Create new key).
# SUBGRAPH_ROUTING_URL: Public URL where this subgraph serves GraphQL (must match what GraphOS will call).

APOLLO_KEY=
APOLLO_GRAPH_REF=RomainRetreat@current
SUBGRAPH_NAME=payload
SUBGRAPH_ROUTING_URL=https://your-public-host/graphql

# Required for `yarn export:subgraph-sdl` (same as romainRetreatServer / Payload):
DATABASE_URL=
PAYLOAD_SECRET=
PAYLOAD_DISABLE_GRAPHQL=1
